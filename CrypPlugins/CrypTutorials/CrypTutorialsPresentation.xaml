<UserControl x:Class="Cryptool.CrypTutorials.CrypTutorialsPresentation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="1200" Background="Black">
    <Grid>
        <Grid.Resources>
            <DataTemplate x:Key="YouTubeItemTemplate">
                <Border Background="Black" BorderBrush="White" BorderThickness="1" Margin="10,10,10,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="500" />
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="350"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="50"/>
                        </Grid.RowDefinitions>
                        <TextBlock Foreground="White" FontSize="14" Margin="10,10,10,10" Grid.Row="0" Text="{Binding Path=Title}" TextAlignment="Left"/>
                        <TextBlock Foreground="White" FontSize="12" Margin="10,10,10,10" Grid.Row="1" Text="{Binding Path=Description}" TextAlignment="Justify" TextWrapping="Wrap"/>
                            <Button Click="PlayVideoButtonClick" Name="PlayVideoButton" Content="{Loc Show_Video}" Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="10,10,10,10"/>
                    </Grid>
                        <Image MouseDown="ImageClick"  Margin="10,10,10,10" Source="{Binding Path=ThumbNailUrl}" Width="300" Height="300" Grid.Column="2"/>
                </Grid>
                </Border>
            </DataTemplate>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Image Source="/CrypTutorials;component/cryptutorials_header.png" Grid.Row="0" />

        <Label Grid.Row="1" MinHeight="40" VerticalContentAlignment="Center" Margin="21,0,12,0">
            <Label.Content>
                <TextBlock Foreground="White" TextWrapping="Wrap" Text="{Loc Welcome_Cryptutorials}" />
            </Label.Content>
        </Label>

        <Grid Margin="21,0,12,0" Name="WebBrowserGrid" Visibility="Hidden"  Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <WebBrowser VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Navigating="WebBrowserNavigating" PreviewKeyDown="WebBrowserOnPreviewKeyDown" Name="WebBrowser" Grid.Row="0" />
            <Button Name="BackButton" Click="BackButtonClick" Content="{Loc Back_to_overview}" Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="10,10,10,10"/>
        </Grid>
        <ScrollViewer Name="ScrollViewer" Grid.Row="2" CanContentScroll="false" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl HorizontalContentAlignment="Stretch" Name="YoutubeVideos" ItemsSource="{Binding}" ItemTemplate="{StaticResource YouTubeItemTemplate}" Margin="21,0,12,0" Background="Black"/>
        </ScrollViewer>
    </Grid>
</UserControl>
