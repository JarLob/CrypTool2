<UserControl x:Class="Wizard.WizardSettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" xmlns:Properties="clr-namespace:Wizard.Properties"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid> 
        <Image Source="/Wizard;component/Images/Hat.png" Opacity="0.2" />
        <StackPanel HorizontalAlignment="Stretch" Name="stackPanel1" VerticalAlignment="Top" Margin="20">
            <CheckBox Content="{Loc RunContent}" ToolTip="{Loc RunToolTip}" Height="16" Name="run" IsChecked="{Binding Source={x:Static Properties:Settings.Default}, Path=RunTemplate, Mode=TwoWay}" Style="{StaticResource ResourceKey=settingsStyle}" />
        </StackPanel>
    </Grid>
</UserControl>
