<UserControl x:Class="KeySearcherPresentation.Controls.StatisticsPresentation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:Controls="clr-namespace:KeySearcherPresentation.Controls" xmlns:System="clr-namespace:System;assembly=mscorlib" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Controls:InformationToProgressConverter x:Key="InformationToProgressConverter"/>
        <Controls:InformationToProgressConverter2 x:Key="InformationToProgressConverter2"/>
    </UserControl.Resources>
    
    <Grid>
        <TreeView HorizontalAlignment="Stretch" Name="statisticsTree" VerticalAlignment="Stretch" ItemsSource="{Binding}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Value}">
                    <HierarchicalDataTemplate.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200" />
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <ProgressBar Value="{Binding Path=Value.Count, Mode=OneTime,Converter={StaticResource ResourceKey=InformationToProgressConverter2}}" Grid.Column="0" Maximum="1" />
                                <TextBlock Text="{Binding Path=Value.Hostname}" Grid.Column="1"/>
                            </Grid>
                        </HierarchicalDataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>                        
                        <ProgressBar Value="{Binding Path=Value, Mode=OneTime, Converter={StaticResource ResourceKey=InformationToProgressConverter}}" Grid.Column="0" Maximum="1" />
                        <TextBlock Text="{Binding Path=Key}" Grid.Column="1"/>
                    </Grid>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>
