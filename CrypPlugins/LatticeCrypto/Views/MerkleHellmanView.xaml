<UserControl x:Class="LatticeCrypto.Views.MerkleHellmanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ViewModels1="clr-namespace:LatticeCrypto.ViewModels"
             xmlns:Properties="clr-namespace:LatticeCrypto.Properties" mc:Ignorable="d" >

    <UserControl.DataContext>
        <ViewModels1:MerkleHellmanViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Name="rowMarkleHellman"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Name="rowLog" Height="55"/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,20,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" >
                        <Label Content="{x:Static Properties:Languages.labelKeyLength}"/>
                        <TextBox Text="{Binding ElementName=scrollBar, Path=Value, Mode=TwoWay}" Width="25" VerticalAlignment="Center" Margin= "5,0,0,0"/>
                        <ScrollBar Name="scrollBar" Maximum="100" Minimum="2" Value="5" SmallChange="1" LargeChange="2" RenderTransformOrigin="0.5,0.5" Margin= "5,0,0,0">
                            <ScrollBar.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform/>
                                    <SkewTransform/>
                                    <RotateTransform Angle="180"/>
                                    <TranslateTransform/>
                                </TransformGroup>
                            </ScrollBar.RenderTransform>
                        </ScrollBar>
                    </StackPanel>

                    <Button Content="{x:Static Properties:Languages.buttonGenerateNewCryptosystem}" Width="auto" Grid.Row="1" Grid.Column="0" Click="Button_Click"/>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <Label Content="{x:Static Properties:Languages.labelPrivateKey}" Grid.Row="0" Grid.Column="0"/>
                    <TextBox Text="{Binding PrivateKey, Mode=OneWay}" IsReadOnly="True" Grid.Row="0" Grid.Column="1" TextWrapping="Wrap" />
                    <Label Content="{x:Static Properties:Languages.labelPublicKey}" Grid.Row="1" Grid.Column="0"/>
                    <TextBox Text="{Binding PublicKey, Mode=OneWay}" IsReadOnly="True" Grid.Row="1" Grid.Column="1" TextWrapping="Wrap" />
                    <Label Content="{x:Static Properties:Languages.labelModulus}" Grid.Row="2" Grid.Column="0"/>
                    <TextBox Text="{Binding Mod, Mode=OneWay}" IsReadOnly="True" Grid.Row="2" Grid.Column="1"/>
                    <Label Content="{x:Static Properties:Languages.labelMultiplier}" Grid.Row="3" Grid.Column="0"/>
                    <TextBox Text="{Binding R, Mode=OneWay}" IsReadOnly="True" Grid.Row="3" Grid.Column="1"/>
                    <Label Content="{x:Static Properties:Languages.labelMultiplierInverse}" Grid.Row="4" Grid.Column="0"/>
                    <TextBox Text="{Binding RI, Mode=OneWay}" IsReadOnly="True" Grid.Row="4" Grid.Column="1"/>
                </Grid>

                <Grid Grid.Row="2" Margin="0,20,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="{x:Static Properties:Languages.buttonEncrypt}" Command="{Binding EncryptCommand}"/>
                        <Button Content="{x:Static Properties:Languages.buttonDecrypt}" Command="{Binding DecryptCommand}" Margin="10,0,0,0"/>
                        <Button Content="{x:Static Properties:Languages.buttonCryptanalysis}" Command="{Binding CryptanalysisCommand}" Margin="10,0,0,0"/>
                    </StackPanel>

                    <Label Content="{x:Static Properties:Languages.labelPlainText}" Grid.Row="1"/>
                    <TextBox Text="{Binding Message, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="700" Height="200" Grid.Row="2"  TextWrapping="Wrap" AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto"/>
                    
                    <Label Content="{x:Static Properties:Languages.labelCiphertext}" Grid.Row="3"/>
                    <TextBox Text="{Binding Cipher, Mode=OneWay}" IsReadOnly="True" Width="700" Height="200" Grid.Row="4" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                </Grid>
            </Grid>
        </ScrollViewer>
        <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" Height="5" ResizeDirection="Rows" Background="DarkGray" MouseDoubleClick="GridSplitter_MouseDoubleClick" ShowsPreview="True"/>
        <Grid Grid.Row="2">
            <RichTextBox Name="History" Background="LightGray" AcceptsReturn="True" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextChanged="History_TextChanged" />
        </Grid>
    </Grid>
</UserControl>
