<UserControl x:Class="Cryptool.FrequencyTest.FrequencyTestPresentation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Cryptool.FrequencyTest"        
    Background="Transparent"
    SnapsToDevicePixels="True" 
    HorizontalAlignment="Center"
             Height="Auto"
             Width="Auto" >
    
    <UserControl.Resources>
        
        <local:DataSource x:Key="source"/>
        
        
        <DataTemplate x:Key="template" >
            
                
               <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" >
                
                   <Rectangle Width="Auto"/>
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center">

                    <TextBlock HorizontalAlignment="Center" Text="{Binding Path=Percent}">
                        %
                    </TextBlock>



                    <Rectangle Height="{Binding Path=Amount}" Width="15" StrokeThickness="1" Stroke="Beige" RadiusX="5" RadiusY="5" >
                        <Rectangle.BitmapEffect>
                            <DropShadowBitmapEffect />
                        </Rectangle.BitmapEffect>

                        <Rectangle.Fill>
                            <RadialGradientBrush >
                                <GradientStop Color="Turquoise" Offset="0" />
                                <GradientStop Color="DarkBlue" Offset="1" />
                            </RadialGradientBrush>
                        </Rectangle.Fill>


                    </Rectangle>

                    <TextBlock HorizontalAlignment="Center" Text="{Binding Path=Caption}"/>


                </StackPanel>
            </StackPanel>
                
        </DataTemplate>
        
        
        <ItemsPanelTemplate x:Key="panelTemplate">
            <StackPanel Orientation="Horizontal"/>
                
        </ItemsPanelTemplate>
        
    </UserControl.Resources>
    
    
   
        <ScrollViewer ScrollViewer.VerticalScrollBarVisibility="Auto"  ScrollViewer.HorizontalScrollBarVisibility="Auto">
        <StackPanel>
        <TextBlock TextAlignment="Center" FontSize="18" Foreground="Blue" FontWeight="Bold" FontFamily="Veranda" OpacityMask="Turquoise">Frequency Test Chart</TextBlock> 
           <ItemsControl 
             
             ItemsSource="{Binding Source={StaticResource source},Path=ValueCollection}"
             ItemTemplate="{StaticResource template}" ItemsPanel="{StaticResource panelTemplate}">
                
            </ItemsControl>
           
                          
        </StackPanel>
    </ScrollViewer>
    
</UserControl>
    
