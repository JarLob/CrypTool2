<UserControl x:Class="WorkspaceManager.View.VisualComponents.CryptoLineView.CryptoLineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:bin="clr-namespace:WorkspaceManager.View.VisualComponents.CryptoLineView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" DataContext="{Binding RelativeSource={x:Static RelativeSource.Self}}">
    <UserControl.Resources>
        <bin:LineOverBindingConverter x:Key="LineOverBindingConverter"></bin:LineOverBindingConverter>
        <Style TargetType="{x:Type Path}" x:Key="Path">
            <Style.Triggers>
                <DataTrigger Value="True">
                    <DataTrigger.Binding>
                        <MultiBinding Converter="{StaticResource ResourceKey=LineOverBindingConverter}">
                            <Binding Path="Target.IsMouseOver"></Binding>
                            <Binding Path="Source.IsMouseOver"></Binding>
                            <Binding Path="IsMouseOver"></Binding>
                        </MultiBinding>
                    </DataTrigger.Binding>
                    <Setter Property="Visibility" Value="Visible"></Setter>
                </DataTrigger>
            </Style.Triggers>
            <Setter Property="Visibility" Value="Collapsed"></Setter>
            <Setter Property="Stroke" Value="RoyalBlue"></Setter>
            <Setter Property="Opacity" Value="1"></Setter>
            <Setter Property="StrokeThickness" Value="5.5"></Setter>
            <Setter Property="StrokeDashArray" Value="4,1,4,1"></Setter>
        </Style>

        <Style TargetType="{x:Type Thumb}" x:Key="Drag">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=IsSelected, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type bin:CryptoLineView}}}" Value="false">
                    <Setter Property="Visibility" Value="Collapsed"></Setter>
                </DataTrigger>
            </Style.Triggers>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border Background="White" BorderBrush="RoyalBlue" Width="8" Height="8" BorderThickness="1"></Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Canvas>
        <Path Data="{Binding Path=PathGeo}" Style="{StaticResource ResourceKey=Path}"></Path>
        
        <ContentPresenter Content="{Binding Path=Line}">

        </ContentPresenter>

        <!--<ItemsControl Margin="-4.5" ItemsSource="{Binding Path=Line.PointList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Thumb Style="{StaticResource ResourceKey=Drag}" DragDelta="ThumbDragDelta" DragCompleted="ThumbDragCompleted">
                        <Thumb.RenderTransform>
                            <TranslateTransform X="{Binding Path=To.X, UpdateSourceTrigger=PropertyChanged}" Y="{Binding Path=To.Y, UpdateSourceTrigger=PropertyChanged}"></TranslateTransform>
                        </Thumb.RenderTransform>
                    </Thumb>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemContainerStyle>
                <Style>
                </Style>
            </ItemsControl.ItemContainerStyle>
        </ItemsControl>-->
    </Canvas>
</UserControl>
