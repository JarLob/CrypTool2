<UserControl x:Class="WorkspaceManager.View.VisualComponents.BottomBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:z="clr-namespace:WorkspaceManager.View.Converter"
    xmlns:p="clr-namespace:WorkspaceManager.Properties"
    Width="auto" MaxWidth="250" Height="30">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resource/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <z:ScaleToPercentageConverter x:Key="PercentageConverter"></z:ScaleToPercentageConverter>
        </ResourceDictionary>
    </UserControl.Resources>
    <UserControl.Effect>
        <DropShadowEffect BlurRadius="10" Color="Black" ShadowDepth="0.5" Direction="90" Opacity="0.4"></DropShadowEffect>
    </UserControl.Effect>

    <Border Background="{StaticResource ResourceKey=BottomBoxBrushLinearBrush}" BorderBrush="#bfd0f5" BorderThickness="0.5" CornerRadius="15,15,0,0">
        <StackPanel Height="35" Orientation="Horizontal" HorizontalAlignment="Center">
            <Border Margin="1,1,1,1" Opacity="0.85" CornerRadius="5,5,0,0" Background="{StaticResource ResourceKey=BottomBoxBackgroundBrush}" Height="25">
                <Label Foreground="#444444" ContentStringFormat="{}{0:F0} %" Content="{Binding Source={x:Static p:Settings.Default}, Path=EditScale, Mode=OneWay, Converter={StaticResource ResourceKey=PercentageConverter}}" Width="45"></Label>
            </Border>
            <Border Margin="1,1,1,1" Opacity="0.85" CornerRadius="5,5,0,0" Background="{StaticResource ResourceKey=BottomBoxBackgroundBrush}" Height="25">
                <Button Margin="5,2,5,0" Style="{StaticResource ResourceKey=ScalePluginContainerButtonStyle}" Click="Button_Click_1">
                    <Image Source="/WorkspaceManager;component/View/Image/reduce.png" Width="18" Height="18"></Image>
                </Button>
            </Border>
            <Border Margin="1,1,1,1" Opacity="0.85" CornerRadius="5,5,0,0" Background="{StaticResource ResourceKey=BottomBoxBackgroundBrush}" Height="25">
                <Slider Name="SliderEditorSize" Ticks="1,2" TickPlacement="BottomRight" Style="{StaticResource ResourceKey=SliderStyle}"
                                    Minimum="0.5" Maximum="2" Width="100" Height="26"
                                    Value="{Binding Source={x:Static p:Settings.Default}, Path=EditScale, Mode=TwoWay}"/>
            </Border>
            <Border Margin="1,1,1,1" Opacity="0.85" CornerRadius="5,5,0,0" Background="{StaticResource ResourceKey=BottomBoxBackgroundBrush}" Height="25">
                <Button Margin="5,2,5,0" Style="{StaticResource ResourceKey=ScalePluginContainerButtonStyle}" Click="Button_Click">
                    <Image Source="/WorkspaceManager;component/View/Image/increase.png" Width="18" Height="18"></Image>
                </Button>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
