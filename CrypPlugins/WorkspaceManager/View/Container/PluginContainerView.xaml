<UserControl x:Class="WorkspaceManager.View.Container.PluginContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:e="clr-namespace:WorkspaceManager.View.Container"
             MinHeight="200" MinWidth="300"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resource/Brushes.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel Name="Root" LastChildFill="True">
        <StackPanel DockPanel.Dock="Right" Name="InputConnectorPanel" Width="20"/>
        <Border DockPanel.Dock="Left" Width="10" Height="{Binding ElementName=PresentationPanel, Path=ActualHeight}" Background="#82bbef" BorderBrush="#4c71bf" BorderThickness="1" Opacity="0.5" Margin="0,10,0,0">
            <StackPanel Name="OutputConnectorPanel"/>
        </Border>
        <Border Background="{StaticResource ResourceKey=GlassBrush}" CornerRadius="7" BorderBrush="#82bbef" BorderThickness="1.3">
            <Grid Name="PresentationBase" Width="Auto" Height="Auto">
                <Grid.RowDefinitions>
                    <RowDefinition Height="20"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Rectangle Margin="10,5,10,5" Height="10" Opacity="0.5" Fill="#82bbef"/>
                <Label Name="PluginName" FontSize="11" FontFamily="Georgia" Foreground="WhiteSmoke">test</Label>
                <Grid Grid.Row="1" Name="PresentationPanel" Width="Auto" Height="Auto" Margin="10,0,10,10" Background="WhiteSmoke">
                    <Grid.Effect>
                        <DropShadowEffect BlurRadius="2" Color="Black" ShadowDepth="0.1" Direction="90" Opacity="0.4"></DropShadowEffect>
                    </Grid.Effect>
                </Grid>
            </Grid>
        </Border>
    </DockPanel>
</UserControl>
