<UserControl x:Class="Cryptool.Alphabets.AlphabetPresentation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="Auto" Width="Auto">
  <Grid Width="Auto" Height="Auto">
    <Grid.RowDefinitions>
      <RowDefinition Height="43"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <GroupBox Grid.Column="0" Name="groupBoxDefaultAlphabet" Header="Current Selected Alphabet">
      <TextBox TextAlignment="Left" IsReadOnly="True" Name="textBoxCurrentAlphabet"></TextBox>
    </GroupBox>

    <GroupBox Name="groupBoxAvailableAlphabets" Header="Available Alphabets" Grid.Row="1">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ListView Grid.Row="0" Name="listViewAlphabets" ItemsSource="{Binding}" SelectionChanged="listViewAlphabets_SelectionChanged">
          <ListView.View>
            <GridView>
              <GridViewColumn Width="60" Header="Default" DisplayMemberBinding="{Binding Default}"/>
              <GridViewColumn Width="150" Header="Name" DisplayMemberBinding="{Binding Name}"/>
              <GridViewColumn Width="350" Header="Alphabet" DisplayMemberBinding="{Binding Alphabet}"/>
            </GridView>
          </ListView.View>
        </ListView>
      </Grid>
    </GroupBox>

    <GroupBox Grid.Row="2" Name="groupBoxManagerAlphabet" Header="Manage Alphabet">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="150"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="5,0,5,0">
          <Label HorizontalAlignment="Left">Name</Label>
          <TextBox TextChanged="textBoxAlphabet_TextChanged" Name="textBoxAlphabetName"/>
        </StackPanel>
        <StackPanel Grid.Column="1" Margin="5,0,5,0">
          <Label HorizontalAlignment="Left">Alphabet</Label>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"  Name="textBoxAlphabet" TextChanged="textBoxAlphabet_TextChanged" MinWidth="150" HorizontalAlignment="Stretch" />
            <Button Grid.Column="1" VerticalAlignment="Bottom" Margin="5,0,0,0" HorizontalAlignment="Stretch" Name="buttonAddAlphabet" Content="Add" Click="buttonAddAlphabet_Click"></Button>
          </Grid>
        </StackPanel>
      </Grid>
    </GroupBox>
  </Grid>
</UserControl>
