<UserControl x:Class="Cryptool.P2P.P2PSettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:p="clr-namespace:Cryptool.P2P">
    <Grid>
        <CheckBox Content="{Loc Use_Timeout}" ToolTip="{Loc Use_Timeout_Tooltip}" IsChecked="{Binding Source={x:Static p:P2PSettings.Default}, Path=UseTimeout, Mode=TwoWay}" Style="{StaticResource ResourceKey=settingsStyle}"/>
    </Grid>
</UserControl>
