<UserControl x:Class="WebCamCap.WebCamCapPresentation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:webcam="clr-namespace:Cryptool.Plugins.WebCamCap" 

             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <webcam:ThreeFourthConverter x:Key="ThreeFourthConverter" />
    </UserControl.Resources>
    <Grid>

        <webcam:CapPlayer Grid.Row="2" x:Name="WebcamPlayer" Stretch="Fill" 
                              Rotation="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Path=WebcamRotation}"
                              Height="{Binding ElementName=WebcamPlayer, Path=ActualWidth, Converter={StaticResource ThreeFourthConverter}}"
                              Device="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Path=SelectedWebcam}" >                   
        </webcam:CapPlayer>
        
    </Grid>
</UserControl>
