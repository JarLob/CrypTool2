<UserControl x:Class="CrypCloud.Manager.Screens.JobList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:CrypCloud.Manager.ViewModels"
             xmlns:converter="clr-namespace:CrypCloud.Manager.Screens.Converter"
             mc:Ignorable="d">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <converter:BoolToVisibilityConverter FalseValue="Collapsed" x:Key="show" />
            <converter:BoolToVisibilityConverter TrueValue="Collapsed" FalseValue="Visible" x:Key="hide" />
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/Colors.xaml" />
                <ResourceDictionary Source="Styles/Button.xaml" />
                <ResourceDictionary Source="Styles/ProgressBar.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <UserControl.DataContext>
        <viewModels:JobListVM />
    </UserControl.DataContext>

    <Grid Visibility="{Binding Path=IsActive, Converter={StaticResource show}}" HorizontalAlignment="Center" Width="610">
        <ListView ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemsSource="{Binding Path=RunningJobs}" x:Name="JobListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Creator" DisplayMemberBinding="{Binding Creator}" Width="100" />
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="100" />
                    <GridViewColumn Header="Progress" DisplayMemberBinding="{Binding NumberOfBlocks}" Width="200" /> 
                    <GridViewColumn Header="" Width="110" >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel  Orientation="Horizontal">
                                    <Button Visibility="{Binding Path=HasWorkspace, Converter={StaticResource hide}}"
                                            Command="{Binding DataContext.DownloadWorkspaceCommand, ElementName=JobListView}" 
                                            CommandParameter="{Binding}" DataContext="{Binding}"
                                            Width="95" Margin="0,5,0,5">
                                        
                                        <StackPanel Orientation="Horizontal" >
                                            <StackPanel Visibility="{Binding Path=IsDownloading, Converter={StaticResource hide}}" Orientation="Horizontal">
                                                <Image Source="../Images/download.png" Width="15"  Margin="0,0,5,0"/>
                                                <TextBlock>Download</TextBlock>
                                            </StackPanel> 
                                            
                                            <StackPanel Visibility="{Binding Path=IsDownloading, Converter={StaticResource show}}" Orientation="Horizontal">
                                                <ProgressBar Height="15" Width="90" IsIndeterminate="True"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Button>
                                    
                                    <Button  Visibility="{Binding Path=HasWorkspace, Converter={StaticResource show}}"
                                             Command="{Binding DataContext.OpenJobCommand, ElementName=JobListView}" 
                                             CommandParameter="{Binding}" DataContext="{Binding}"
                                             Width="75" Margin="0,5,0,5">
                                        <StackPanel Orientation="Horizontal" >
                                            <Image Source="../Images/open.png" Width="10"  Margin="-5,0,5,0"/>
                                            <TextBlock>Open</TextBlock>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>   
                    
                    <GridViewColumn Header="" Width="100" >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Visibility="{Binding Path=UserCanDeleteJob, Converter={StaticResource show}}" 
                                            Command="{Binding DataContext.DeleteJobCommand, ElementName=JobListView}" 
                                            CommandParameter="{Binding}" DataContext="{Binding}"
                                            Width="75" Margin="5,5,0,5">
                                        <StackPanel Orientation="Horizontal" >
                                            <Image Source="../Images/delete.png" Width="10"  Margin="-5,0,5,0"/>
                                            <TextBlock>Delete</TextBlock>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                </GridView> 
            </ListView.View> 
        </ListView>
        
        <!-- bottom controls -->
        <Grid VerticalAlignment="Bottom" >
            <Button Content="Refresh" Command="{Binding Path=RefreshJobListCommand}"
                    VerticalAlignment="Bottom" 
                    HorizontalAlignment="Left" 
                    Margin="5,5,0,5"  
                    Height="23"
                    Width="75"/>

            <Button Content="New" Command="{Binding Path=CreateNewJobCommand}" 
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Margin="0,5,5,5"  
                    Height="23"
                    Width="75"/>

        </Grid>
    </Grid>
</UserControl>
